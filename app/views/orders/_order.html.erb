<dl>
  <dt class="card-title">
    <strong><%= Order.human_attribute_name(:code) %>: </strong>
    <%= link_to order.code, order %>
  </dt>
  <dd class="card-subtitle mb-2 text-muted">
    <strong><%= Order.human_attribute_name(:event_date) %>: </strong>
    <%= I18n.localize(order.event_date) %>
  </dd>
</dl>